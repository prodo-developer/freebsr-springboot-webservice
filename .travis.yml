#.Travis.yml
language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_install:
  - chmod +x gradlew

before_deploy:
  - zip -r freebsr-springboot-webservice *
  - mkdir -p deploy
  - mv freebsr-springboot-webservice.zip deploy/freebsr-springboot-webservice.zip

deploy:
  - provider: s3
    access_key_id: AKIA57OHDUUD4USHNVMH
    secret_access_key: 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
    bucket: freebsr-springboot-build
    region: ap-northeast-2
    skip_cleanup: true
    acl: private
    local_dir: deploy
    wait-until-deployed: true

  - provider: codedeploy
    access_key_id: AKIA57OHDUUD4USHNVMH
    secret_access_key: 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
    bucket: freebsr-springboot-build
    key: freebsr-springboot-webservice.zip # 빌드 파일을 압축해서 전달
    bundle_type: zip
    application: freebsr-springboot-webservice # 웹 콘솔에서 등록한 CodeDeploy 어플리케이션
    deployment_group: freebsr-springboot-webservice-group # 웹 콘솔에서 등록한 CodeDeploy 배포 그룹
    region: ap-northeast-2
    wait-until-deployed: true
#CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - wjdals300@gmail.com